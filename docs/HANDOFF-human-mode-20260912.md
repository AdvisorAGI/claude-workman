# Handoff: workman human mode, owner priority, hand-back, remote (2026-09-12)

Owner order: stop spending Fable. Tree left consistent; nothing committed (owner rule).
Suite: `cd ~/workman && .venv/bin/python -m pytest tests -q -p no:cacheprovider` -> **591 passed, 0 failed** (5.63 s; 2026-09-12 after grok-4.6 verifier/fixer completed finding 7). Was 575 after Opus, 506 before findings 1-7.
Uncommitted files: 41 (`git status --short`); the `?? ops/`, `?? workman/{account,cu_memory,cu_skills,episode,learned_shortcuts,memory_graph,shortcuts,working,workspace_layout}.py`, `?? tests/test_{episode,memory,stability}.py` entries are ANOTHER session's work, untouched except `tests/test_stability.py:96` (fake `type_text` gained `dwell_ms=0`).

Running now on the DGX: `workman-esc-pause.service` (user unit, enabled, pid 1774985, `:0`, XI2 raw, XTEST ids [4,5], no grab). Debug-port Chrome pid 1061727 (profile google-chrome-teardown, :9222) untouched and still running; workman never uses that port (`chrome.cdp_enabled` off unless `WORKMAN_CHROME_CDP=1`). No `workman.remote --relay` process is running (see NOT DONE R1). Xvfb `:99` (pid 22316) still up, owned by an earlier session.

## (a) DONE

| item | where | test |
|---|---|---|
| Persistent XTest channel (no process per action): move/button/click/type/combo/screenshot via XGetImage, keymap + scratch keycodes, XQueryKeymap, XI1 device state, XI2 raw events, WM_DELETE, DPMS | `workman/xtest.py` (Channel 213-913; `type_text` 624, `press_combo` 581, `needs_remap` 615, `release_xtest_held` 789, `core_keys_down` 713, `select_raw_events` 869, `close_window` 817, `wake_screen` 847) | `tests/test_xtest.py` (19) |
| x11 routes through the channel with xdotool fallback; browser-aware typing (paste instead of scratch remap); foreign-pointer detection | `workman/x11.py:58,75,276,292,301,533` | `tests/test_event_shape.py::TestKeyboardShape`, `tests/test_x11.py` |
| Lite-mode numbers (Xvfb :99, `scripts/bench_lite.py`, 100-char string fixed) | before/after table below | bench script |
| Owner priority: stamps, `human_active`, `wait_for_human_quiet`, `_safe` yields then refuses `human_active`; releases never held; Escape switch refuses `input_disabled` | `workman/owner_pause.py:126,195,234,240,257`; `workman/desktop.py:30` | `tests/test_owner_priority.py` (14) |
| Escape listener: native XI2, RawInputWatcher (physical vs XTEST-claimed vs KVM), release on Escape, start-with-input-on, pid file, CLI `--status/--resume/--pause/--release` | `workman/esc_pause.py:103,146,192,258,339` | `tests/test_esc_listener.py` (16) |
| systemd user unit installed + enabled on the DGX; repo copies + Mac plist | `~/.config/systemd/user/workman-esc-pause.service`, `ops/esc-pause/{workman-esc-pause.service,ai.atmosphere.workman-esc-pause.plist,README.md}` | `tests/test_esc_listener.py::TestUnitFiles` |
| Generic human timing: Fitts' law duration, min-jerk, tremor, overshoot on long moves, 60-125 Hz sampling, log-normal IKI + dwell, fast bigrams, word/sentence pauses, hover before click, 60-140 ms hold, Gaussian aim in target, seedable rng | `workman/human.py:28-65 (constants), 170 motion_steps, 178 jitter_point, 194 human_click_element, 224 path_duration_ms, 266 eased_path, 342 typing_cadence, 381 key_dwell_ms, 497 human_press_click, 635 human_type` | `tests/test_human.py` (40: Fitts, noise band, tremor, lognormal, bigrams, pauses, density, no-teleport, min-jerk), `tests/test_event_shape.py::TestPointerShape/TestKeyboardShape` |
| Human Mode default on (`apply_session_default`) | `workman/human.py:126`, `server.py:1535` | `tests/test_reviewer_items.py::TestHumanModeDefault` |
| CDP off by default, challenge detection, `chrome.click_text` propagates failure | `workman/chrome.py:45,50,64,69,77` | `tests/test_chrome.py` (27 incl. `test_cdp_is_off_by_default_and_never_touches_the_port`, `test_challenge_titles_are_recognised`), `test_reviewer_items.py::TestFailurePropagation` |
| launch_app refuses automation flags (debug port, --enable-automation, --headless, --user-data-dir, ...) unless `WORKMAN_ALLOW_AUTOMATION_FLAGS=1` | `workman/apps.py:58,65,86` | `test_reviewer_items.py::TestLaunchGuards` |
| Browser a11y is FIND-only: click_element jittered pointer click; perform_action -> pointer in browsers, do_action native; set_value -> click, ctrl+a, human type in browsers, editable_text native | `workman/atspi.py:115,140,156,172,271,308,337` | `tests/test_event_shape.py::TestBrowserA11yIsFindOnly` |
| Server: `_input` wrapper (bot-challenge refusal, pointer note, interruption memory) on every input tool; `input_control`, `resume_interrupted`, `hand_back`, `remote`, `remote_screenshot`, `remote_status`; `workman_platform` reports owner_pause, listener pid, chrome_cdp; launch tracking; `_NeverRaised`; `_click_with_human` release-safe | `workman/server.py:27,86,100,252,441,567,628,671,713,722,742,1192,1225,1248,1268,1281,1295` (95 tools) | `tests/test_resume.py` (14), `tests/test_handback.py` (13), `test_reviewer_items.py` (12), `tests/test_server.py` |
| Resume from where it stopped (typing remainder + stray-char fix, drag from drop point, remaining wheel ticks, click target); resume needs `owner_confirmed=True` | `workman/resume.py:32,53` | `tests/test_resume.py` |
| Hand-back: release tracked + XTEST-held (verified with the server), close only launched windows via WM_DELETE (pid tree), park pointer unless owner active, wake screen, checklist | `workman/handback.py:97,202,248,307` | `tests/test_handback.py` |
| Remote module: relay + streamed `wm.py serve` over one ssh child, transport reported from fleet-connect | `workman/remote.py:78,127,208,311,338,347` | `tests/test_remote.py` (13) |
| `wm.py` rewritten: lazy backend, `--host`/`WM_HOST`, `serve`, `ping`, `pause`, `resume`, `handoff`, `status`, `stop`, `WM_NO_REEXEC`, image_b64 for path `-`; `fleet-wm.sh` (device or all, shots to `~/.claude/skills/workman/shots/<device>-<verb>-<UTC>.png`) | `~/.claude/tools/wm.py`, `~/.claude/tools/fleet-wm.sh` | manual: `wm.py ping` (0.30 s), `echo '{"verb":"ping","args":[]}' \| WM_NO_REEXEC=1 ~/workman/.venv/bin/python ~/.claude/tools/wm.py serve` (ready line + result) |
| Detector page (isTrusted, pointer density/intervals/teleports, hold, key dwell/flight, event.code, Shift on capitals, navigator.webdriver, verdict in title) | `tests/fixtures/humanness.html` | run live on `:99` 2026-09-12: HUMAN (L1) |
| Test guard: conftest stubs channel, learn root, challenge check, pointer note | `tests/conftest.py` | whole suite |

### Lite-mode numbers (Xvfb :99, 2560x1440, `WORKMAN_LEARN_ROOT=/tmp/workman-learn-99`)

| action | before (xdotool) wall / CPU / spawns | after (XTest) wall / CPU / spawns |
|---|---|---|
| human_move 600 px | 927 ms / 125.8 ms / 73 | 802 ms / 23.1 ms / 0 |
| human_type 100 chars | 11838 ms / 272.7 ms / 100 | 20192 ms / 52.6 ms / 0 (wall is the new log-normal cadence + word pauses, by design) |
| screenshot (native) | 135 ms / 94.3 ms / 2 | 71 ms / 70.5 ms / 0 |
| screenshot view (max 1568) | 202 ms / 164.8 ms / 2 | 43 ms / 41.1 ms / 0 |

Remote, mac-mini over the direct cable 169.254.81.2 (AFTER measured 2026-09-12 by Opus 5 after the R1 fix; read-only verbs only, no input verbs sent to the Mac):

| verb | BEFORE (one ssh + fresh wm.py per action) | AFTER CLI wall, warm relay (`python3 wm.py --host mac-mini <verb>`) | AFTER in-process relay RTT (`remote.call`) |
|---|---|---|---|
| ping | not measured | 26-41 ms (cold, relay start: 1098 ms) | 0.7-1.3 ms |
| status | (platform: 1038 ms) | 28 ms, `transport` "direct cable 169.254.81.2", `rtt_ms` 1.6 | n/a |
| shot | 438 ms | 420-549 ms | 239-293 ms |
| windows | 489 ms | 131-148 ms | 101-135 ms |
| key shift / type '' | 367 / 367 ms | not measured (no input verbs on the mac-mini by order) | not measured |

Shot AFTER includes carrying the ~1.0 MB PNG back to the DGX as base64 plus the local save and blank check (about 180 ms of the CLI wall); whether BEFORE carried the image back is not recorded, so the shot row is not like for like.

## (b) NOT DONE

**R1. DONE 2026-09-12 (Opus 5).** Two causes. (1) `remote.relay_command()` now builds the relay argv + env: the repo venv python when present, and always the package root on `PYTHONPATH`; `wm.py:_remote_module()` exports the same `PYTHONPATH`. (2) With the interpreter fixed, `status` still showed `transport: null`: the relay's ssh rode another session's ControlMaster (`~/.ssh/cm-tariqul@mac-mini:22`), which skips the fleet-connect ProxyCommand that logs the path; `remote.SSH` now passes `-o ControlMaster=no -o ControlPath=none`. Tests: `tests/test_remote.py::TestNames::test_relay_child_imports_the_package_under_a_bare_interpreter`, `test_spawn_relay_passes_that_env_and_detaches`, `test_relay_ssh_never_rides_a_shared_control_master`. Acceptance met: cold 1098 ms, warm 26-41 ms, transport "direct cable 169.254.81.2" (numbers in the remote table above). Relay stopped afterwards. Original note:
**R1 (original). Remote relay does not start from `wm.py --host` (BLOCKER, Opus 5).** `workman/remote.py:276 _spawn_relay` runs `sys.executable -m workman.remote`; when wm.py is run by `/usr/bin/python3` the child has no `workman` on its path -> `~/.cache/workman/remote/mac-mini.relay.log`: "No module named workman.remote", then 25 s timeout. Fix: in `_spawn_relay` prefer `~/workman/.venv/bin/python` when it exists, else pass `env={**os.environ, "PYTHONPATH": str(Path(__file__).resolve().parents[1])}`; also make `wm.py:_remote_module()` export the same PYTHONPATH. Acceptance: `python3 ~/.claude/tools/wm.py --host mac-mini ping` returns `ok` in < 3 s cold and < 100 ms warm; `wm.py --host mac-mini status` shows `transport` "direct cable 169.254.81.2"; then record AFTER RTTs for ping/shot/windows/key shift/type '' and put them in the table above. Unit test: patch `remote.subprocess.Popen` and assert the argv interpreter exists or PYTHONPATH is set.

**R2. Remote path on the mac-mini needs the new wm.py semantics (Grok 4.6 xhigh).** The Mac still has the 2026-09-03 `~/.claude/tools/wm.py`; the relay streams the DGX copy so nothing must be installed, but `fleet-wm.sh`/docs on the Mac are stale. Command (classifier may block writes over ssh; if so list it for the owner): `scp ~/.claude/tools/wm.py ~/.claude/tools/fleet-wm.sh mac-mini:~/.claude/tools/`. Acceptance: `ssh mac-mini '~/.claude/tools/wm.py ping'` prints backend `atmos`.

**R3. Mac listener install (owner action; files ready).** `scp ~/workman/workman/{esc_pause,owner_pause,xtest}.py mac-mini:~/workman/workman/ && scp ~/workman/ops/esc-pause/ai.atmosphere.workman-esc-pause.plist mac-mini:~/Library/LaunchAgents/ && ssh mac-mini 'launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/ai.atmosphere.workman-esc-pause.plist'` then grant Accessibility to the interpreter named in the plist (owner toggle). Note the Mac `~/workman` carries 5 uncommitted `ops/computer-use-lab/*` files of another session; do not overwrite anything else there. Acceptance: `ssh mac-mini '~/workman/.venv/bin/python -m workman.esc_pause --status'` shows `listener_pid`.

**L1. DONE 2026-09-12 (Opus 5): verdict HUMAN, every acceptance number met, no code change needed.** Harness `/tmp/l1_harness.py` (ephemeral; it asserts `x11.DISPLAY`, the x11 channel and `xtest.channel()` are all `:99` before any input). Deviations from the recipe, all for safety: snap chromium has a private `/tmp`, so the throwaway profile was `mktemp -d` under `~/snap/chromium/common` (same path on both sides, removed afterwards); added `--no-default-browser-check --password-store=basic --ozone-platform=x11` so no keyring prompt and no Wayland surface can reach the owner's `:0`. Colour boxes are found as the largest solid block: a plain `getbbox()` also catches subpixel-antialiased text of the same colour (run 1 did that, so its target/field clicks probably missed the elements; its verdict was HUMAN too: hold 61-106 ms, dwell min 60 ms, 0 teleports, 0 untrusted). Run 2 (correct boxes: target 260x90, field 432x30; `/tmp/l1-field.png` shows "Hello World, 42!" in the field): title `HUMAN`, JSON:
`{"navigator_webdriver": false, "events_total": 162, "events_untrusted": 0, "pointer_samples": 115, "pointer_interval_ms": {"n": 113, "min": 9, "median": 19, "mean": 17.9, "max": 210}, "pointer_step_px": {"n": 114, "min": 0, "median": 4.5, "mean": 5.7, "max": 18.6}, "pointer_teleports_over_80px": 0, "click_hold_ms": {"n": 3, "min": 99, "median": 102, "mean": 113.7, "max": 140}, "move_samples_in_500ms_before_click": {"n": 3, "min": 22, "median": 24, "mean": 24, "max": 26}, "key_count": 19, "key_dwell_ms": {"n": 19, "min": 46, "median": 80, "mean": 85.1, "max": 136}, "key_flight_ms": {"n": 18, "min": -136, "median": 54, "mean": 56.1, "max": 213}, "key_flight_overlaps": 3, "keys_with_empty_code": 0, "capitals": 2, "capitals_with_shift": 2, "wheel_events": 0, "wheel_intervals_ms": null, "flags": [], "verdict": "nothing here distinguishes this from a person"}`
Cleanup: SIGTERM to the one chromium pid (1915759), no leftovers, profile dir removed. Note: `click_hold_ms.max` 140 sits exactly on the `CLICK_PRESS_MS` upper edge (page-measured hold equals the injected press). The 3 flight overlaps are Shift rollover. Original plan: Approach: `DISPLAY=:99 chromium-browser --user-data-dir=$(mktemp -d) --no-first-run --window-size=1400,900 --window-position=0,0 file:///home/monzurul/workman/tests/fixtures/humanness.html &` (launched directly, NOT through launch_app, which refuses `--user-data-dir` by design; no debug port); with `WORKMAN_DISPLAY=:99 WORKMAN_LEARN_ROOT=/tmp/workman-learn-99` and `human.set_mode(True, seed=7)`: screenshot, find the #target box by colour `#cfe8ff`, the field by `#fff2cc`, the Summarise button by `#d9f7be`; `human.human_click` the box, click the field, `human.human_type("Hello World, 42!")`, click Summarise, read `xtest.channel(":99").active_window_info()["name"]`: it starts with `HUMAN {json}` or `SCRIPTED {json}`. Then `kill` that chromium pid and `rm -rf` the temp dir. Write the JSON into this file. Acceptance: title starts with `HUMAN`, `events_untrusted` 0, `pointer_teleports_over_80px` 0, `capitals_with_shift == capitals`, `keys_with_empty_code` 0, `click_hold_ms` within 60-140, `key_dwell_ms.min >= 35`. Never on `:0` and never in the owner's Chrome.

**L2. SKIPPED 2026-09-12 (Opus 5), fail closed; still open.** Owner checks passed: listener pid 1774985, switches on, `agent_held` empty, `human_input_age_s` null, Mutter idle 4631 s, screen not locked. But the focused window on `:0` was Remmina "Mac mini screen (via ssh tunnel)" (1280x1011 at 130,118) with the pointer over it (703,542). Launching zenity takes Remmina's focus, and a Remmina keyboard grab could route the typed text to the mac-mini. hand_back's park move would also land inside the VNC viewer, which forwards pointer motion to the Mac. Both break the hard limits (no input to the mac-mini, never touch Remmina). Re-run when Remmina is not focused and the pointer is outside it, or with the owner present; add "active window is not a remote viewer" to the pre-check. Original plan:
**L2 (original). Live :0 typing + hand_back check (Opus 5; owner may be at the desk).** Approach: with the listener running, `server.launch_app("zenity --entry --title=workman-typing-check")` (tracked), `layout_focus("workman-typing-check")`, `server.type_text("workman ok")`, zoom to read it, then `server.hand_back()` and assert `clean` true and the zenity window gone, pointer back where it was. `xterm` is not installed on the DGX; zenity/gedit/gnome-text-editor are. Acceptance: `hand_back()["checks"]` all true, `input_control("status")["agent_held"]` empty.

**L3. Real stdio start of the server (Grok).** `cd ~/workman && timeout 5 .venv/bin/python -m workman.server </dev/null; echo $?` should print the "workman: ... unavailable" lines at most and exit on EOF without a traceback; then re-register the `workman` MCP server in the session (it failed to connect at the start of this session, most likely from the earlier `apply_session_default` crash, now fixed).

**D1. Docs (Grok).** Append dated lines to `~/.claude/skills/workman/LEARNINGS.md`: XTest channel vs xdotool numbers; XWarpPointer (Deskflow) invisible to XI2 raw so foreign-pointer motion is the complement; select XIAllMasterDevices or every raw event arrives twice; CDP off by default and the debug Chrome pid is not workman's; `launch_app` refuses automation flags; hand_back/resume/input_control exist. Add a "Owner priority and Escape" section to `~/.claude/skills/workman-human/SKILL.md` (before "Learned lessons", line 61) and a fleet paragraph to `~/.claude/skills/workman/SKILL.md` "Fleet" (line 38) describing `wm.py --host`, `fleet-wm.sh`, `remote_*` tools. Acceptance: files mention `input_control`, `resume_interrupted`, `hand_back`, `remote_status`, `WORKMAN_CHROME_CDP`.

**H1. Server-level tests for the remote tools (Grok).** `server.remote/remote_screenshot/remote_status` with `server.remote` patched: error dict on RemoteError, `[meta, Image]` on bytes. File: `tests/test_server.py`.

**H2. DONE 2026-09-12 (Opus 5), fakes only, not run on a Mac.** `platform/darwin.py:639 release_all()` is the backend's answer to "what beyond the tracked holds is safe to release": nothing, `verified: None`, `released_blind: False`. Quartz has no per-source held state, so a blind modifier-up could cancel a key the owner is holding. `handback.py:96 _backend_attr` + `:138`: with no X channel, a backend that has the hook is asked instead of the blind ctrl/alt/shift/super + buttons 1-3 release. A hook that raises is reported as an error with `verified: False`, never raised. Tracked holds still go up through the existing `desktop.key_up/mouse_up` path, which is Quartz on the Mac. Tests in `tests/test_handback.py`: `TestMacHandBack::test_releases_only_tracked_holds_and_reports_unverified` (real darwin module on a fake Quartz: exactly one key-up, keycode 56, and one left-up; `clean` True; `screen.checked` False) and `test_darwin_hook_contract`; `TestHandBack::test_a_failing_platform_hook_is_reported_not_raised`. Original note:
**H2 (original). hand_back on macOS (Opus).** `handback.release_all` has no channel on darwin: add a `release_all` hook in `platform/darwin.py` (CGEvent key/button up for tracked holds) and skip `wake_screen` gracefully (already returns `checked: False`). Acceptance: `hand_back()` on the Mac returns `verified: None` with no traceback.

**H3. Human-mode `press_key` per-key dwell (Grok, small).** `server.press_key` -> `desktop.press_key` -> `Channel.press_combo(hold_ms=0)`: pass `hold_ms=human.key_dwell_ms()` when Human Mode is on (`x11.press_key` needs a `hold_ms` kwarg; `desktop._accepts` pattern at `desktop.py:185`). Acceptance: test that `press_combo` is called with 35 <= hold_ms <= 180 in human mode.

**Reviewer findings 1-8 status.** 1 (`_NeverRaised`) done + tested; 2 (`apply_session_default`) done + tested; 3 (chrome/atspi propagation) done + tested; 4 (`mouse_button` bail) done + tested; 5 (drag/press release outcome) done + tested; 6 (`_click_with_human` key_up raising) done + tested; 7 (AIM pause always) done, covered by `test_event_shape::test_click_is_many_moves_a_pause_then_hold_then_release`; 8 (path density) done + tested. Nothing of 1-8 remains.

## (e) Verifier findings 1-7 (Grok 4.6 xhigh), fixed 2026-09-12 by Opus 5, re-verified and completed by grok-4.6

Suite after Opus: **575 passed, 0 failed**. Suite after this grok-4.6 round: see the pytest summary in the verifier report (this session). Fakes only: nothing sent to `:0`, no relay started, no ssh past `mac-mini echo ok`, nothing committed.

| # | status | fix | tests |
|---|---|---|---|
| 1 | CONFIRMED FIXED | `handback.note_launch(result, before)` records, AT LAUNCH, the window ids absent from the `before` snapshot and owned by the launch pid tree; `_our_windows` closes only those ids (no pid walk at hand_back). `server.launch_app` takes the snapshot (`handback.window_ids()`). | `test_handback.py::TestCloseLaunched::test_a_pre_existing_window_of_the_same_app_survives`, `test_a_window_opened_after_the_launch_is_never_ours`, `test_without_a_before_snapshot_only_the_reported_window_is_ours` |
| 2 | CONFIRMED FIXED | `chrome.click_text` aims with `human.human_click_element(el)` (Gaussian point in the box). | `test_chrome.py::TestReadClickWait::test_click_text_aims_inside_the_box_not_the_centre` |
| 3 | CONFIRMED FIXED | `ax_darwin.is_web_element` (browser/Electron app name via `darwin.is_browser_app`, or an `AXWebArea` ancestor): click and perform_action = pointer click at a jittered point in the frame; set_value = click, `super+a`, human_type. Native apps keep AXPress/AXValue. | `tests/test_ax_darwin.py` (10) |
| 4 | CONFIRMED FIXED | darwin `US_CHARS`/`char_key` (US virtual keycodes), `_stroke` (real modifier key down/up around the key, held, released in reverse in a `finally`), `press_key(hold_ms)`, `type_text(dwell_ms)`. Off-layout characters are pasted in a browser and the clipboard is restored. | `tests/test_darwin_input.py` |
| 5 | CONFIRMED, NOT CHANGED | Reasoning holds: sshd holds no Screen Recording, so `~/workman/.venv` Quartz capture over ssh is blank; the daemon-backed computer-mcp venv is the only remote route that can take screenshots. `wm.py` lines 10-11 document that this venv forwards every verb to Workman.app. Do not reorder until the owner grants Screen Recording to the workman venv python (or capture is routed through the daemon). **Open owner checks (ssh fence):** (a) confirm the computer-mcp venv cannot `import workman.server` (otherwise wm.py would pick workman.server first and capture would go blank); (b) on the mini, the daemon's input guards apply, not workman's `desktop._safe` (viewer guard, owner pause). | `test_remote.py::TestNames::test_remote_pythons_prefer_the_daemon_venv` |
| 6 | CONFIRMED FIXED | relay `_shutdown` verb; `stop()` asks over the socket first, else SIGTERMs only a confirmed relay pid; SIGTERM raises SystemExit so `finally` unlinks sock/json. | `test_remote.py::TestStop` (7) |
| 7 | FIXED (Opus incomplete; completed this round) | Focus guard as Opus shipped, plus: (a) `click`/`click_with`/`drag`/`hover`/`scroll_at`/`mouse_down` refuse with `remote_viewer_under_target` when the topmost client under that point is a viewer (`xtest.Channel.window_at_point` via `_NET_CLIENT_LIST_STACKING` + frame geometry; darwin `window_at_point` via the Quartz list). `move` and releases are not point-checked (lite). (b) With no channel and no backend hook, one xdotool `getactivewindow getwindowclassname` (name only if class empty). Keys and point-guarded clicks fail closed (`remote_viewer_unknown`) when that read fails; pointer moves still go through. conftest stubs `_xdotool_focused` so pytest never talks to `:0`. (c) `is_remote_viewer` matches class/instance/app; the title is used only when those are empty. | `tests/test_remote_viewer_guard.py`, `test_xtest.py::TestWindowAtPoint`, `test_darwin_input.py::TestFrontmost::test_window_at_point_hits_an_unfocused_viewer` |

Accepted gaps (not blocking): (1) a window the launched app opens after `launch_app` returns is not closed (fails safe: hand_back leaves it open rather than closing one of the owner's); `wait_for_window=0` records nothing; a launch that hands off to an already-running process never adopts that process's pre-existing windows. (3) perform_action in a browser is a left click for any action name (parity with atspi). (4) on a non-US layout the keycodes are US positions carrying the intended unicode string, so the text is right but event.code follows US positions. (7) `move` is still not point-checked, so a human path that *crosses* an unfocused viewer still forwards motion; the 200 ms move cache is only for the focused-window verdict; override-redirect fullscreen viewers missing from `_NET_CLIENT_LIST_STACKING` still have focus, so the focus guard covers them. Mac-side live checks need the R3 listener install. Not run on a Mac.

## (c) Known risks and half-written pieces

- `remote.py` relay ran end to end against the mac-mini on 2026-09-12 with read-only verbs only (ping/status/shot/windows); input verbs over the relay are still unexercised live. (Fixed 2026-09-12, finding 6: `stop` now asks the relay over its socket, SIGTERM runs the relay's cleanup, and a stale json pid is never signalled.)
- `human_type` wall time doubled (20.2 s / 100 chars) because of the owner's log-normal cadence with word/sentence pauses; if too slow, lower `IKI_LOGNORM` mean (170 ms) or `SPACE_FACTOR` (1.6) in `human.py:28,42`; tests read the constants, not literals.
- `_input` wrapper calls `chrome.challenge_active()` on every input tool: one EWMH read on the channel, but on the xdotool fallback path it is three xdotool spawns per action. Acceptable on the DGX (channel), worth a cache on boxes without libX11.
- `handback.note_launch` walks `/proc` for the launched pid tree once, at launch time (finding 1); on macOS it only matches the launch pid itself.
- The listener shares `~/.grok/workman-learn` stamps with every workman process on the box; a `:99` harness must set `WORKMAN_LEARN_ROOT` or the owner's real activity makes it yield (done in the bench).
- `tests/test_stability.py` (another session's untracked file) was edited in one line to the new `type_text` signature.
- `ops/` at the repo root is untracked and shared with another session; only `ops/esc-pause/` is mine.

## (d) Verify commands

```
cd ~/workman && .venv/bin/python -m pytest tests -q -p no:cacheprovider          # see latest verifier report
systemctl --user status workman-esc-pause.service --no-pager                        # active, pid
cd ~/workman && .venv/bin/python -m workman.esc_pause --status --display :0         # listener_pid, agent_held empty, input_channel xtest
WORKMAN_LEARN_ROOT=/tmp/workman-learn-99 WORKMAN_DISPLAY=:99 WORKMAN_XTEST=0 .venv/bin/python scripts/bench_lite.py   # before
WORKMAN_LEARN_ROOT=/tmp/workman-learn-99 WORKMAN_DISPLAY=:99 .venv/bin/python scripts/bench_lite.py                   # after
python3 ~/.claude/tools/wm.py ping                                                 # local, re-exec into the venv
echo '{"verb":"ping","args":[]}' | WM_NO_REEXEC=1 ~/workman/.venv/bin/python ~/.claude/tools/wm.py serve
python3 ~/.claude/tools/wm.py --host mac-mini ping                                 # ok: cold ~1.1 s, warm ~30 ms (R1 done)
python3 ~/.claude/tools/wm.py --host mac-mini status                               # transport "direct cable 169.254.81.2"; then `stop`
ps -o pid,args -p 1061727 | cut -c1-120                                            # debug Chrome still the owner's, untouched
.venv/bin/python -c "import workman.server as s; print(len(s.mcp._tool_manager._tools))"   # 95
```
